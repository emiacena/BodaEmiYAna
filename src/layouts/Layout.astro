---
interface Props {
  title?: string;
}
const { title = 'Nuestra Boda' } = Astro.props;
---
<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Os invitamos a celebrar nuestra boda" />
    <title>{title}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Moontime&family=Ovo&family=Open+Sans:wght@300;400;500&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  *, *::before, *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  :root {
    --color-bg: #F0EEE9;
    --color-surface: #E4DED6;
    --color-text: #545454;
    --color-text-light: #A49C90;
    --color-accent: #6E7B63;
    --color-accent-dark: #4A5442;
    --color-white: #FFFFFF;
    --color-border: #CEC8C0;
    --font-script: 'Moontime', cursive;
    --font-serif: 'Ovo', Georgia, serif;
    --font-sans: 'Open Sans', system-ui, sans-serif;
    --section-padding: 5rem 1.5rem;
    --max-width: 900px;
  }

  html { scroll-behavior: smooth; }

  body {
    font-family: var(--font-sans);
    color: var(--color-text);
    background-color: var(--color-bg);
    font-weight: 300;
    line-height: 1.7;
    -webkit-font-smoothing: antialiased;
  }

  h1 {
    font-family: var(--font-script);
    font-weight: 400;
    line-height: 1.2;
  }

  h2, h3, h4 {
    font-family: var(--font-serif);
    font-weight: 400;
    line-height: 1.3;
  }

  a {
    color: var(--color-accent);
    text-decoration: none;
  }

  a:hover { color: var(--color-accent-dark); }

  img { max-width: 100%; height: auto; display: block; }

  .container {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  .section-title {
    font-size: clamp(1.8rem, 4vw, 2.5rem);
    text-align: center;
    margin-bottom: 0.75rem;
    color: var(--color-text);
  }

  .section-divider {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    margin-bottom: 3rem;
  }

  .section-divider::before,
  .section-divider::after {
    content: '';
    display: block;
    width: 60px;
    height: 1px;
    background: var(--color-accent);
    opacity: 0.5;
  }

  .section-divider span {
    color: var(--color-accent);
    font-size: 0.9rem;
  }

  /* Scroll reveal */
  .reveal {
    opacity: 0;
    transform: translateY(24px);
    transition: opacity 0.7s ease, transform 0.7s ease;
  }

  .reveal.visible {
    opacity: 1;
    transform: none;
  }
</style>

<script>
  const observer = new IntersectionObserver(
    (entries) => entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('visible'); }),
    { threshold: 0.1, rootMargin: '0px 0px -40px 0px' }
  );
  document.querySelectorAll('.reveal').forEach(el => observer.observe(el));
</script>
